---
import BaseLayout from '../layouts/BaseLayout.astro';

const releaseLinks = [
  {
    label: 'Release notes',
    title: 'GitHub releases',
    body: 'Published notes, tags, and artifacts for public compiler drops.',
    href: 'https://github.com/blacklight-foundation/ultraviolet/releases',
    cta: 'Open releases',
  },
  {
    label: 'Build guide',
    title: 'Local compiler build',
    body: 'Prerequisites and bootstrap steps for building the compiler locally.',
    href: '/docs/build-the-compiler/',
    cta: 'Open build guide',
  },
  {
    label: 'First run',
    title: 'Start with the quickstart',
    body: 'Create a manifest, write a first source file, and run the command surface.',
    href: '/docs/quickstart/',
    cta: 'Open quickstart',
  },
];

const releaseTimeline = [
  {
    title: 'Build checklist',
    status: 'Now',
    tone: 'now',
    body: 'Smoke tests, prerequisite checks, and documented verification for local builds.',
  },
  {
    title: 'Packaged artifacts',
    status: 'Next',
    tone: 'next',
    body: 'Target-specific archives, checksums, and installation notes for the first packaged alpha.',
  },
  {
    title: 'Release cadence',
    status: 'Planned',
    tone: 'planned',
    body: 'Supported-target policy, recurring release notes, diagnostics examples, and public reports.',
  },
];
---

<BaseLayout
  title="Compiler Releases"
  description="Find the current Ultraviolet compiler release path, build instructions, GitHub artifacts, and public alpha release status."
>
  <article class="article-page status-page releases-page">
    <header>
      <p class="eyebrow">Compiler downloads</p>
      <h1>Compiler Releases</h1>
      <p>
        Find compiler artifacts, release notes, build instructions, and
        quickstart links for the public alpha.
      </p>
      <div class="actions">
        <a class="button primary" href="https://github.com/blacklight-foundation/ultraviolet/releases">
          Open GitHub releases
        </a>
        <a class="button secondary" href="/docs/build-the-compiler/">
          Open build guide
        </a>
      </div>
    </header>

    <section class="card release-current" aria-labelledby="release-status-heading">
      <div>
        <span class="status-kicker">Available now</span>
        <h2 id="release-status-heading">Current alpha path.</h2>
      </div>
      <p>
        Build from source today; packaged binaries are being prepared.
      </p>
      <a class="button secondary" href="/docs/build-the-compiler/">
        Open build guide
      </a>
    </section>

    <section class="status-page-section">
      <h2>Release paths</h2>
      <div class="status-card-grid">
        {
          releaseLinks.map((link) => (
            <a class="card post-card compact-card status-link-card" href={link.href}>
              <span class="status-tag">{link.label}</span>
              <h3>{link.title}</h3>
              <p>{link.body}</p>
              <span class="card-cta">{link.cta}</span>
            </a>
          ))
        }
      </div>
    </section>

    <section class="status-page-section">
      <h2>Release timeline</h2>
      <div class="timeline">
        {
          releaseTimeline.map((item) => (
            <article class="timeline-item">
              <div>
                <h3>{item.title}</h3>
                <p>{item.body}</p>
              </div>
              <span class={`status-tag status-${item.tone}`}>{item.status}</span>
            </article>
          ))
        }
      </div>
      <p>
        See the <a href="/roadmap/">public alpha roadmap</a> for funded milestones
        and delivery status.
      </p>
    </section>
  </article>
</BaseLayout>
