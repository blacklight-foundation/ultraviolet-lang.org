---
import '../styles/site.css';
import { oneTimeDonation } from '../data/funding';
import {
  siteDescription,
  siteTitle,
  socialImageAlt,
  socialImageHeight,
  socialImagePath,
  socialImageType,
  socialImageWidth,
} from '../data/siteMetadata.mjs';

interface Props {
  title?: string;
  description?: string;
}

const defaultTitle = siteTitle;
const defaultDescription = siteDescription;

const { title = defaultTitle, description = defaultDescription } = Astro.props;
const pageTitle =
  title === defaultTitle ? defaultTitle : `${title} | ${defaultTitle}`;
const site = Astro.site ?? new URL('https://ultraviolet-lang.org');
const canonical = new URL(Astro.url.pathname, site).toString();
const socialImage = new URL(socialImagePath, site).toString();

const navLinks = [
  { href: '/docs/', label: 'Docs' },
  { href: '/releases/', label: 'Releases' },
  { href: '/playground/', label: 'Playground' },
  { href: '/roadmap/', label: 'Roadmap' },
  { href: '/blog/', label: 'Updates' },
  {
    href: 'https://github.com/blacklight-foundation/ultraviolet',
    label: 'GitHub',
  },
];

const footerPrimaryLinks = [
  { href: '/docs/', label: 'Docs' },
  { href: '/releases/', label: 'Releases' },
  { href: '/playground/', label: 'Playground' },
  { href: '/roadmap/', label: 'Roadmap' },
  { href: '/blog/', label: 'Updates' },
  { href: '/sponsor/', label: 'Support' },
  {
    href: 'https://github.com/blacklight-foundation/ultraviolet',
    label: 'GitHub',
  },
];

const footerUtilityLinks = [
  { href: '/foundation/', label: 'Foundation' },
  { href: oneTimeDonation.href, label: 'Donate' },
  { href: 'mailto:hello@blacklight.foundation', label: 'Contact' },
  {
    href: 'https://github.com/blacklight-foundation/ultraviolet/blob/main/LICENSE.md',
    label: 'License',
  },
  {
    href: 'https://github.com/blacklight-foundation/ultraviolet/blob/main/SPECIFICATION.md',
    label: 'Specification',
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Ultraviolet" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:secure_url" content={socialImage} />
    <meta property="og:image:type" content={socialImageType} />
    <meta property="og:image:width" content={socialImageWidth} />
    <meta property="og:image:height" content={socialImageHeight} />
    <meta property="og:image:alt" content={socialImageAlt} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    <meta name="twitter:image:alt" content={socialImageAlt} />
    <script is:inline src="/newsletter-signup.js" defer></script>
    <slot name="head" />
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to content</a>
    <header class="site-header">
      <a class="brand" href="/" aria-label="Ultraviolet home">
        <span class="brand-mark" aria-hidden="true">
          <img
            src="/images/ultraviolet_darkmode_transparent.png"
            width="1000"
            height="1000"
            alt=""
          />
        </span>
        <span class="brand-text">Ultraviolet</span>
      </a>
      <nav class="site-nav" aria-label="Primary navigation">
        {
          navLinks.map((link) => (
            <a href={link.href}>
              {link.label}
            </a>
          ))
        }
      </nav>
      <div class="header-actions">
        <a class="nav-secondary" href="/docs/">Start</a>
        <a class="nav-cta" href="/sponsor/">Support</a>
      </div>
    </header>
    <main id="content">
      <slot />
    </main>
    <footer class="site-footer">
      <div class="footer-inner">
        <div class="footer-primary">
          <a class="footer-brand" href="/">
            <span class="footer-brand-mark" aria-hidden="true">
              <img
                src="/images/ultraviolet_darkmode_transparent.png"
                width="1000"
                height="1000"
                alt=""
              />
            </span>
            <span>Ultraviolet</span>
          </a>
          <p class="footer-summary">
            High-visibility, general-purpose programming language for AI and
            Humans, stewarded by Blacklight Foundation.
          </p>
          <nav class="footer-links" aria-label="Footer primary navigation">
            {
              footerPrimaryLinks.map((link) => (
                <a href={link.href}>
                  {link.label}
                </a>
              ))
            }
          </nav>
        </div>
        <div class="footer-secondary">
          <form
            class="footer-signup"
            data-newsletter-form
            action="/api/subscribe"
            method="post"
            novalidate
          >
            <input type="hidden" name="source" value="footer" />
            <label class="sr-only" for="footer-newsletter-email">
              Email address
            </label>
            <input
              id="footer-newsletter-email"
              name="email"
              type="email"
              autocomplete="email"
              inputmode="email"
              placeholder="Email for public alpha updates"
              required
            />
            <input
              class="newsletter-honeypot"
              name="company"
              type="text"
              tabindex="-1"
              autocomplete="off"
              aria-hidden="true"
            />
            <button type="submit">Subscribe</button>
            <p
              class="footer-signup-status"
              data-newsletter-status
              aria-live="polite"
              aria-atomic="true"
            >
            </p>
          </form>
          <nav class="footer-links footer-utility-links" aria-label="Footer utility navigation">
            {
              footerUtilityLinks.map((link) => (
                <a href={link.href}>
                  {link.label}
                </a>
              ))
            }
          </nav>
          <div class="footer-meta">
            <span>&copy; 2026 Blacklight Foundation</span>
            <span>Currently in alpha.</span>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
